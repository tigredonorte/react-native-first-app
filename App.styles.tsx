import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    margin: 10,
    marginTop: 50,
    marginBottom: 0
  },
  input: {
    borderColor: '#000',
    borderWidth: 1,
    flex: 1,
    padding: 0,
    marginRight: 5,
    paddingLeft: 10,
  },
  inputContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
  },
  itemsContainer: {
  }
});
